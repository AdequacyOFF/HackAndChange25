version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:5173"
    restart: unless-stopped
    networks:
      - site-network

networks:
  site-network:
    driver: bridge